# 安全策略

## 支持版本

全部工作区组件的首个公开版本统一为 `1.0.0`。在 `1.0.0` tag 创建前，主开发分支属于该版本的
发布候选；tag 创建后，`1.0.x` 接收兼容性修复和安全修复，新的破坏性公开 API 变更进入下一 major
版本。

| 版本 | 安全支持 |
| --- | --- |
| `1.0.x` | 支持 |
| `< 1.0.0` | 未公开发布，不支持 |

## 漏洞报告

发现安全问题时，请使用仓库的
[Private vulnerability reporting](https://github.com/nasa-runtime/nasa-runtime-rust/security/advisories/new)
私密提交。正式 tag 创建前必须确认该入口已在仓库设置中开启并可用。

不要在修复可用前公开提交漏洞细节。若私密入口暂时不可用，只能创建不含技术细节、凭据、复现路径
或受影响目标的普通 issue，请求维护者恢复私密报告入口。

安全敏感问题包括：

- WebSocket、REST、调度或管理入口相关代码中的认证或授权绕过；
- 日志、调试输出、指标、错误信息、示例或 README 片段泄露密钥；
- mapper 宏中的 SQL 注入或不安全动态 SQL 展开；
- 缓存一致性问题导致租户数据串读；
- 事务问题导致 after-commit 动作在错误时机发布；
- Redis 命令路由或 Stream 行为写入错误 key 或错误 group；
- 无界队列、任务、缓冲区、payload、图片尺寸或连接数导致拒绝服务；
- 服务发现或负载均衡把请求路由到不健康或已禁用实例；
- 现代加密入口被错误使用，导致认证加密语义失效。

## 安全默认值

- Mapper SQL 默认使用绑定参数；原始 SQL 片段必须通过显式白名单能力，例如类型化 order-by 辅助函数。
- 事务内使用共享 L2 缓存时必须显式声明，避免未提交视图被误写入共享缓存。
- WebSocket 服务端配置应包含连接数、未认证连接、payload、发送队列和 handler 并发上限。
- Redis Stream 和分区消费关键路径应返回可见错误状态，不能静默丢弃。
- 配置加载器遇到必填非法值应快速失败，并且不能打印凭据。
- 兼容加密能力只用于老系统互通；新系统应使用现代认证加密入口。
- PKCS#1 v1.5 私钥解密和历史私钥运算默认不编入可执行路径；受控迁移必须显式启用
  `legacy-rsa-private`，Web 端点还必须同时通过 provider 运行时风险门。`nasa/full` 不开启该能力。

## 依赖策略

发布前运行供应链检查：

```bash
cargo deny check
```

如果必须临时忽略安全公告，需要说明：为什么相关依赖仍然需要保留、受影响执行路径是否默认可达、
风险如何被限制，以及什么条件满足后可以移除忽略项。
