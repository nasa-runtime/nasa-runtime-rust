# 贡献指南

`nasa-runtime-rust` 是多 crate 工作区。业务应用优先依赖 `nasa` 门面，再按需开启 feature。内部 crate
保留给实现、宏展开和渐进迁移，公开示例优先展示门面用法。

## 变更检查

提交代码前，根据影响范围运行相应命令：

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo clippy --workspace --all-targets --all-features -- -D warnings
RUSTDOCFLAGS="-D missing_docs -D warnings" cargo doc --workspace --no-deps
cargo check -p nasa --features full --all-targets
```

涉及依赖许可、安全通告或归档内容时执行 `cargo deny check` 和离线归档检查。涉及真实后端语义时，
使用根级本地质量工程覆盖事务、超时、重投、故障恢复和资源清理；其中的内容、命令和路径不属于公开
仓库。

本地容器可以复现协议与故障语义，但不能代表候选硬件容量。生产峰值、副本拓扑、ACL、在线 DDL 和
灾难恢复结论由目标环境负责人批准。

## 文档规则

- 每个组件 crate 都维护独立 `README.md`，说明用途、依赖或 feature 接入、初始化、yml、正常用法和
  主要边界。
- 根 README 只承担门面使用指南和组件索引；组件细节写入对应 README。
- README、rustdoc、源码注释、SQL 注释和 manifest 注释只描述当前业务能力、配置、安全不变量、边界
  与失败后果。
- 公开叙述不记录内部质量活动、执行结果、发布历史、审查过程、自动化助手归因、无关技术栈类比、
  临时调试内容或禁用表述。
- 真实 API、协议字段和配置键按源码保留原名；“定义版本”“实例版本”“配置版本”等当前业务语义
  可以正常使用。
- 公开文档与注释不使用 `V<数字>-<数字>`、日期前缀或同类内部批次标签；沿革由 Git 提交记录承担。
- 数据库迁移 SQL 使用语义化名称，默认文件名不编码日期或发布代次，结构沿革以 Git 提交记录为准。
- 不写入真实密钥、本地密码、私有主机名、内部网络地址或业务数据。

## 注释规则

- 根级本地质量工程完全豁免本节要求，其中的函数、辅助代码、脚本和 fixture 可以不写注释。
- 注释使用中文，专有名词、协议名、配置键和代码标识符保留原文。
- “版本类字样”仅指 `V<数字>-<数字>` 及同类内部里程碑标签；“调度时刻的实例版本”“定义版本”
  “配置版本”等真实业务语义合法，必须按实现准确描述。
- 注释涉及根级质量工程时，只能陈述“仅本地使用、不进入公开仓库或产品归档”，不得用所有权标签
  为其命名。
- 所有函数和方法注释包含准确的“业务作用”；新增或实质修改的业务函数按实际签名补充“参数说明”
  和“返回”。
- 保护态切换、权威获取或失去、fencing、promotion、动态配置、checkpoint、回滚、补偿和故障恢复前，
  写明业务原因、执行顺序与失败影响。
- 注释不得逐字翻译代码，也不得包含内部质量过程、发布历史、审查归因、无关技术栈类比或临时说明。
- 行为、参数、返回语义或时序变化时同步维护注释，不得保留失效接口或过期路径。

## 代码规则

- 改动收敛在目标组件，避免顺手重构无关模块。
- 沿用现有门面和 feature 布局，不新增不必要的跨 crate 依赖。
- 修改宏行为时，若生成代码对使用者可见，同步更新宏 crate 与运行时 crate README。
- 修改缓存或事务行为时，明确一致性语义、提交边界和失败处理。
- 修改 Redis、MySQL、Kafka、WebSocket、服务发现或调度行为时，文档说明超时、队列、批量、并发或
  资源上限。
- 配置解析对未知字段、非法值和冲突组合尽早失败。
- 公共 API 变化同步更新门面重导出、rustdoc、README 和使用示例。

## 产品资产边界

根级本地工程统一管理质量用例、fixture 和故障注入工具。组件 crate 不携带专用质量目录、内部接口、
脚本、凭据、私钥或证书；这些内容不进入产品归档。公开归档只包含产品源码、公开文档、许可证与必要
的再分发文件。

维护时重点确认：

- feature 单独开启和组合开启均能编译；
- 宏生成代码使用正确门面路径；
- 事务内缓存和外部副作用具有明确可见性；
- 消息确认、DLT、重投和提交不确定状态不会静默丢失；
- 后台任务具有有界队列、取消信号和停机预算；
- 公开文档与真实行为、默认值和资源边界一致。
